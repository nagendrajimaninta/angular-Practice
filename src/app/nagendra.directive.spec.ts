import { NagendraDirective } from './nagendra.directive';

describe('NagendraDirective', () => {
  it('should create an instance', () => {
    const directive = new NagendraDirective();
    expect(directive).toBeTruthy();
  });
});
